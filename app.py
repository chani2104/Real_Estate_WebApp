import streamlit as st

st.set_page_config(page_title="부동산 통합 가이드", layout="wide")

st.title("🏠 부동산 데이터 통합 플랫폼")
st.markdown("---")

col1, col2 = st.columns(2)

with col1:
    st.subheader("📊 지역 인프라 분석")
    st.write("전국 시군구의 인프라(학교, 지하철, 병원 등)와 평균 임대 시세를 분석합니다.")
    if st.button("인프라 분석 바로가기"):
        st.switch_page("pages/1_인프라_분석.py")

with col2:
    st.subheader("📍 실시간 매물 지도")
    st.write("네이버 부동산 데이터를 바탕으로 현재 올라온 실시간 매물을 지도에서 확인합니다.")
    if st.button("매물 지도 바로가기"):
        st.switch_page("pages/2_실시간_매물_지도.py")

st.info("왼쪽 사이드바를 통해서도 페이지 이동이 가능합니다.")
